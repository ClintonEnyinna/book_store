<nav class="navbar navbar-expand navbar-light bg-light justify-content-between">
  <div class="container">
    <%= link_to 'Book Store', root_path, class: 'navbar-brand' %>
    <div class="d-flex justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item active">
          <%= link_to 'Home', root_path, class: 'nav-link' %>
        </li>
        <% if current_user %>
          <% if current_user.is_a?(Seller)%>
            <li class="nav-item">
              <%= link_to 'New Book', new_book_path, class: 'nav-link' %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to 'My Books', books_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Cart', buyer_orders_path(current_user.id), class: 'nav-link' %>
            </li>
          <% end %>
          <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle text-dark" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <%= current_user.name %>
              </a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <%= link_to "Profile", current_user.is_a?(Seller)? seller_path(current_user) : buyer_path(current_user), class: 'dropdown-item' %>
                <%= link_to "Log out", logout_path, method: :delete, class: 'dropdown-item' %> 
              </div>
            </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>